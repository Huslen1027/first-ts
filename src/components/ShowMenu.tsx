type ObjType = {
  name: string;
  dateOfYear: string;
  isVerified: boolean;
  tag: string;
  description: string;
};

type PropType = {
  id: number;
  title: string;
  buttonText: string;
  contents: Array<ObjType>;
};

import { Card } from "./Card";

export const ShowMenu = ({}) => {
  return <></>;
};
